{
  "$schema": "../gen/schemas/desktop-schema.json",
  "identifier": "apps",
  "description": "Remote web apps may only call the notification bridge",
  "webviews": ["app-*"],
  "remote": { "urls": ["https://*", "http://*"] },
  "permissions": ["allow-bridge-notify"]
}
